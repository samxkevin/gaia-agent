Tool implementations will live here.

Planned tools:

- web search
- webpage retrieval
- local file inspection
- structured data analysis
- Python execution
- optional vision capable model support

Keep tool interfaces small and deterministic so the agent can compose them reliably.
